_PRINTF(3)

NAME
		_printf

LIBRARY
		Library stdlib, stdiolib, stdarg, unistd

SYNOPSIS
		int _printf(const char *format, ...);

DESCRIPTION
		The _printf() function produce output according to a format.
		The _printf() write output to stdout, the standard output stream.
		This fucntion write the ouput, according to a format string that how subsequent arguments.

	Format
		'c'		return integer or char.
				write a char.

		's'		if argument is a string. Function will print a string.

		'%'		print a percent

		'd'		if argument is a decimal. Function will print a integer (decimal).

		'i'		if argument is a interger. Function will print integer.

RETURN VALUES
		These functions return the number of characters printed. It return a negative value if an errors occurs.

EXAMPLES
		To print a string, in the form "C is fun !":
		#include "main.h"

		int	main(void)
		{
			/*code here*/
		}

		To print a decimal
		

COMPILATION
		Files compile with Ubuntu 20.04 LTS using gcc, using the options -Wall -Werror -Wextra -pedantic -std=gnu89

BUGS

AUTHORS
		Audrey Lican and Alexey Chernov
